/**
 * App-wide configuration constants.
 *
 * DONATE_URL points at the project's Donorbox page. Donations are how
 * PrivateBudget is funded without compromising privacy: it's an external link
 * (opened in a new tab), so no third-party scripts run inside the app and the
 * strict no-network guarantee stays intact.
 *
 */
export const DONATE_URL = 'https://donorbox.org/private-budget'
